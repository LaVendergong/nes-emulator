# Agent Handbook

这是本仓库给 AI 与人类的同一份操作手册。新会话先读本文，再读 `docs/project-map.md`。提示词原文在 `prompts/`。

本仓库是 NTSC/PAL/Dendy FC/NES 模拟器：Windows 与 Android 两个 Host，核相同。模块边界与不变量以地图为准。

## 目标

长期可维护，不是一次写完。默认假设：代码会活过当前作者、当前模型、当前会话。

## 不可妥协

1. **边界先于功能**：改代码前先指出所属模块与不允许跨越的边界。
2. **最小可合入 diff**：一次只做一个意图；拒绝顺手重构、顺手抽象。
3. **不变量写下来**：核心行为用测试或最小自检钉住；没有钉的行为视为可被改丢。
4. **决策留痕**：不可逆选择（存储、协议、模块切开、依赖引入）写入 `docs/adr/`。
5. **删除优于添加**：能删配置、能删分支、能用标准库，就不新增层。

## 工作流（每次任务）

1. **定位**：这是冷启动 / 架构 / 实现 / 变更 / 审查 / 重构 / 排障 / 交接，哪一种？套对应 `prompts/` 模板。
2. **读图**：打开 `docs/project-map.md`，标出将改的模块与波及面。
3. **动手**：只改清单内的文件；越界先停，问要不要扩 scope。
4. **留下痕迹**：更新地图、ADR、或交接摘要里需要后人知道的那一两句。

## 文档约定

| 文件 | 作用 |
|---|---|
| `docs/project-map.md` | 活地图：模块、入口、不变量、禁止事项 |
| `docs/adr/NNNN-title.md` | 架构决策，只记不可逆选择 |
| `prompts/` | 给人和 AI 复制用的提示词 |

地图不存在时：先用「冷启动」或「架构」模板生成，再写代码。
