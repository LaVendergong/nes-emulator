---
description: 改代码前的影响分析与最小 diff 纪律
alwaysApply: true
---

# 变更协议

动手前用四行回答（写在回复开头，不要写成长文）：

1. **意图**：用户要的结果，一句话。
2. **模块**：改哪些目录/入口；明确不改哪些。
3. **风险**：可能破坏的不变量或调用方。
4. **验证**：用什么命令/检查证明没坏。

然后只做清单内的最小改动。

- 发现 scope 必须扩大：停下来说明原因，等确认。
- 修 bug 先复现再改；改完用同一路径验证。
- 重构与行为变更拆开；混合时只做行为变更。
- 不要为「以后可能」预留接口、配置项、间接层。
