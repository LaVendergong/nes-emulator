---
description: 大型项目长期可维护的硬约束，每次会话都生效
alwaysApply: true
---

# 项目宪法

你在维护一份会活很久的代码，不是在写演示。

- 先定位模块再改。说不清所属边界时，先更新 `docs/project-map.md`，再写代码。
- 一次一个意图。禁止顺手重构、顺手加抽象、顺手升级依赖。
- 能用语言/平台已有能力就用；新依赖必须能说出「没有它做不到什么」。
- 跨模块调用只走已有公开入口。需要新入口时，先写清契约再实现。
- 核心不变量（协议、状态机、数据格式、并发假设）必须有最小可运行检查。
- 不可逆决策写 `docs/adr/`：选了什么、没选什么、何时该推翻。
- 改完后更新地图里被你碰过的那几行。文档与代码同 diff 提交。
